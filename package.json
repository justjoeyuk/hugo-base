{
  "name": "Hugo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -Rf public/ && gulp prod && hugo server -wv",
    "start": "rm -Rf public/ && gulp prod && hugo server -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^4.0.0",
    "gulp-hash": "^4.1.1",
    "gulp-sass": "^3.1.0"
  }
}
